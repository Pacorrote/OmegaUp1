#include <stdio.h>
#include <math.h>

int main(int argc, const char * argv[]) {
    int a;
    scanf("%d", &a);
    if (a % 2 == 0)
        printf("Par");
    else
        printf("Impar");
    return 0;
}
